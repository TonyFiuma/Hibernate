spring:
  datasource:
    url: jdbc:mysql://localhost:3306/develhope_prove
    username: root
    password: password
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: create-drop
